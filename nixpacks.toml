[phases.setup]
nixPkgs = ["nodejs_22", "curl", "wget"]
aptPkgs = []

[phases.install]
cmds = ["NPM_CONFIG_PRODUCTION=false npm ci --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
